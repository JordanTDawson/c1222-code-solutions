/* exported getLastChar */
/* create new function getLastChar with one parameter
   return the string array with length of the string being subtracted by 1 */
function getLastChar(string) {
  return string[string.length - 1];
}
