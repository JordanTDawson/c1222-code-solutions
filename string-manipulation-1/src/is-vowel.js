/* exported isVowel */
/* create a function named isVowel with one parameter charater
   return with logical or statement with character strictly equalling a uppercase
   or lowercase vowel
   close function code block */
function isVowel(character) {
  return character === 'A' || character === 'E' || character === 'I' || character === 'O' || character === 'U' || character === 'a' || character === 'e' || character === 'i' || character === 'o' || character === 'u' || false;
}
