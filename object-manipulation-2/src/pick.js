/* exported pick */
/* create a function named pick with two arguments source and keys
   create a variable an empty object
   create an if statement with a condition that checks that key values from source
   if the condition is true send the key to new object
   close function codeblock and return the new Object. */
function pick(source, keys) {
  var pickedObjects = {};
  return pickedObjects;
}
