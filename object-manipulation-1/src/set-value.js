/* exported setValue */
/* create a function named SetValue with three parameters object key and value
   parameter object at key = value
   return the object to the function code block */
function setValue(object, key, value) {
  object[key] = value;
  return object;
}
