/* we want to select all span elements in html.
   create a new array for all the span elements to be pushed into.
   push all the items from span into the new array.
   create a document event listener that listens for keydown
   create an if statement  */
var span = document.querySelectorAll('span');

var strArray = [];
strArray.push(span);
